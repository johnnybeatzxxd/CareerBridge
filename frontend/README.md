# Frontend

React + Tailwind frontend for the job listing site.

## Commands

```bash
npm install
npm run dev
npm run build
```

The development server runs at `http://localhost:5173` and calls the backend at `http://localhost:8080/api` by default.
